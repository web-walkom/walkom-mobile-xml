<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Walkom"
        tools:targetApi="31">
        <activity
            android:name=".presentation.screens.excursions.ExcursionsActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:theme="@style/Theme.Walkom.CustomStatusNavigationBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ExcursionActivity"
            android:exported="true"
            android:theme="@style/Theme.Walkom.CustomStatusNavigationBar" />
        <activity
            android:name=".presentation.screens.welcome.WelcomeActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.screens.map.MapActivity"
            android:exported="false"
            android:theme="@style/Theme.Walkom.TranslucentActionAndStatusBar" />
        <activity
            android:name=".presentation.screens.camera.CameraARActivity"
            android:exported="false" />
    </application>

</manifest>